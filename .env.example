# Firebase Client SDK Configuration
# Find these in your Firebase project settings > General
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"

# Firebase Admin SDK Configuration (for server-side operations)
# Paste the entire JSON content of your service account key file here.
# It's recommended to store this as a secret in your hosting environment.
FIREBASE_SERVICE_ACCOUNT_KEY='{"type": "service_account", "project_id": "...", ...}'

# API & Function URLs
# The public URL of your deployed 'prospect' Firebase Function.
# For local dev with Firebase Emulator, this is typically:
PROSPECTING_FUNCTION_URL="http://127.0.0.1:5001/your-project-id/us-central1/prospect"

# A secret API key you create for authenticating with the /api/job-status endpoint
STATUS_API_KEY="your-secret-api-key-here"

# The public URL of your deployed crawler service
# For local dev, this would be http://localhost:8080/crawl if you run it locally
CRAWLER_SERVICE_URL="http://localhost:8080/crawl"
