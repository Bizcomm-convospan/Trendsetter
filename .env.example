# Firebase Service Account Key (for Admin SDK - backend)
# This is a JSON key that you can generate in your Firebase project settings under "Service accounts".
# It's a large JSON object. It must be enclosed in single quotes.
# Example: FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account",...}'
FIREBASE_SERVICE_ACCOUNT_KEY=

# Firebase Client-side Configuration (for connecting from the browser)
# You can find these values in your Firebase project settings under "General".
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# WordPress Webhook Integration
# This is used for publishing generated articles to your WordPress site.
# You'll need a custom plugin or endpoint on your WP site to receive this webhook.
# For local testing, you can use a service like ngrok to expose your local endpoint.
WP_WEBHOOK_URL=https://your-ngrok-url-or-live-site.com/wp-json/trendsetter/v1/publish
WP_WEBHOOK_TOKEN=your_secure_token_here

# API Security Keys
# A secret key to protect the /api/job-status endpoint.
STATUS_API_KEY=your-secret-api-key-here

# Deployed Service URLs
# These are the URLs of your deployed Firebase Functions and Crawler service.
# For local testing with emulators, you might use localhost URLs.
PROSPECTING_FUNCTION_URL=http://your-firebase-cloud-function-url/prospect
CRAWLER_SERVICE_URL=http://your-crawler-service-url/crawl
