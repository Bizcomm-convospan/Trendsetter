
import { KeywordStrategyClient } from '@/components/dashboard/keyword-strategy/KeywordStrategyClient';
import { AppLayout } from '@/components/layout/AppLayout';

export default function KeywordStrategyPage() {
    return (
        <AppLayout>
            <KeywordStrategyClient />
        </AppLayout>
    );
}
